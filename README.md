# Финансовый чат-бот с RAG системой

Консольный чат-бот для работы с российскими финансовыми новостями, системной статистикой и временем в Москве.

## 🚀 Быстрый старт

```bash
# Настройте .env файл (см. раздел "Настройка переменных окружения")
cp .env.example .env
nano .env

# Запуск интерактивного чат-бота
docker compose run --rm chatbot
```

## 🚀 Возможности

- **RAG поиск** по базе российских финансовых новостей (150+ статей)
- **Системная статистика** - загрузка CPU и памяти
- **Московское время** - текущее время в часовом поясе Москвы
- **Интеллектуальная маршрутизация** запросов через OpenAI Function Calling
- **Консольный интерфейс** с логированием

## 🏗️ Архитектура

```
src/
├── models/           # Pydantic модели данных
├── services/         # Бизнес-логика и сервисы
│   ├── data_service.py     # ChromaDB + загрузка данных
│   ├── system_service.py   # Системная информация
│   └── llm_service.py      # OpenAI API + Function Calling
├── utils/           # Конфигурация и логирование
└── chatbot.py       # Главное консольное приложение
```

### Технологический стек

- **Python 3.11+** - основной язык
- **GigaChat API** (через совместимость с OpenAI SDK) — по умолчанию; поддержан fallback на OpenAI
- **Hugging Face sentence-transformers** — локальные эмбеддинги без внешних вызовов
- **ChromaDB** - векторная база данных для RAG
- **Docker** - контейнеризация
- **Pydantic** - валидация данных
- **Pandas** - обработка CSV данных

## 📋 Требования

- Docker 20.10+
- Docker Compose 2.0+
- OpenAI API ключ

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd chatbot_project
```

### 2. Настройка переменных окружения

```bash
# Копируем шаблон настроек
cp .env.example .env

# Редактируем .env: GigaChat OAuth параметры (или OpenAI ключ), а для эмбеддингов — модель HF
GIGACHAT_CLIENT_ID=<УКАЗАТЬ_СВОЙ>
GIGACHAT_CLIENT_SECRET=<УКАЗАТЬ_СВОЙ>
```

Файл `.env` может содержать (вариант GigaChat, рекомендуется):
```env
## Вариант А: укажите пару, ключ соберётся автоматически
GIGACHAT_CLIENT_ID=your_client_id
GIGACHAT_CLIENT_SECRET=your_client_secret

## Вариант Б: укажите готовый ключ Base64 (перекрывает вариант А)
# GIGACHAT_AUTH_KEY=base64(ClientID:ClientSecret)
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
GIGACHAT_BASE_URL=https://gigachat.devices.sberbank.ru/api/v1
GIGACHAT_VERIFY_SSL=false

# Модели
LLM_MODEL=GigaChat-2-Pro
# Эмбеддинги: лёгкая мультиязычная модель (поддержка RU)
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
```

Либо (fallback на OpenAI):
```env
OPENAI_API_KEY=your_openai_api_key_here
LLM_MODEL=gpt-4.1-mini-2025-04-14
EMBEDDING_MODEL=text-embedding-3-small
```

### 3. Запуск через Docker Compose

```bash
# Интерактивный запуск (рекомендуемый)
docker compose run --rm chatbot

# Постоянный запуск сервиса (для тестов/разработки)
docker compose up --build

# Примечание: при `docker compose up` интерактивность не работает.
# Для взаимодействия используйте `docker exec`:
docker exec -it financial_chatbot bash
# и запустите бота внутри контейнера при необходимости:
# python src/chatbot.py
```

### 4. Использование

После запуска вы увидите приветственное сообщение и сможете задавать вопросы:

```
🤖 Добро пожаловать в финансового чат-бота!

Примеры вопросов:
• 'Найди новости о Сбербанке'
• 'Какая загрузка процессора?'
• 'Сколько сейчас времени в Москве?'

👤 Ваш вопрос: Покажи статистику системы
```

## 🧪 Тестирование RAG системы

### Запуск тестов качества

```bash
# Способ 1: Через временный контейнер (рекомендуемый)
docker compose run --rm chatbot python tests/test_rag_quality.py

# Способ 2: Если контейнер запущен постоянно
# Сначала: docker compose up --build
# Затем:
docker exec -it financial_chatbot python tests/test_rag_quality.py

# Способ 3: Локально (после установки зависимостей)
cd tests
python test_rag_quality.py
```

### Метрики качества

Система оценивается по следующим метрикам. Они помогают понять как точность, так и устойчивость выдачи:

- **Precision@5**: доля релевантных документов среди первых пяти найденных. Отражает точность топ‑выдачи (чем выше, тем меньше нерелевантных в топ‑5).
- **Recall@5**: доля найденных релевантных документов среди всех ожидаемых релевантных. Отражает покрытие (чем выше, тем больше нужного попадает в топ‑5).
- **Среднее время ответа**: среднее время выполнения запроса поиска. Характеризует производительность.
- **Среднее косинусное сходство**: средняя косинусная схожесть между запросом и найденными документами (диапазон [-1..1], выше — ближе по смыслу; отрицательные значения возможны и означают антикорреляцию).
- **Среднее косинусное расстояние**: среднее косинусное расстояние (диапазон [0..2], ниже — лучше). Для удобства схожесть также вычисляется как `1 - distance`.

#### Интерпретация метрик (быстрые ориентиры)

- Precision@5: ≥ 0.6 — хорошо, 0.3–0.6 — приемлемо, < 0.3 — требует улучшений.
- Recall@5: ≥ 0.6 — хорошо, 0.3–0.6 — приемлемо, < 0.3 — требует улучшений.
- Среднее время ответа: ≤ 1.5 c — хорошо, 1.5–3 c — приемлемо, > 3 c — оптимизировать.
- Косинусное сходство: ближе к 1 — лучше; около 0 — нейтрально; отрицательное — нерелевантно.
- Косинусное расстояние: ближе к 0 — лучше; > 1 — часто сигнал нерелевантности.

Порог прохождения автотестов в проекте: Precision@5 ≥ 0.2, Recall@5 ≥ 0.1, среднее время ответа ≤ 10 c на тест.

### Пример результата тестирования

```
📊 СВОДКА РЕЗУЛЬТАТОВ ТЕСТИРОВАНИЯ
✅ Успешных тестов: 4/5 (80.0%)
📈 Средний Precision@5: 0.742
📈 Средний Recall@5: 0.658
⏱️  Среднее время ответа: 2.34 секунд
🎯 Среднее косинусное сходство: 0.823 ([-1..1])
🧭 Среднее косинусное расстояние: 0.177 ([0..2])
```

## 📁 Структура данных

### Формат финансовых новостей

Данные хранятся в CSV формате со следующими полями:

```csv
id,reasoning,answer,article_text,sphere,source,date
1,"Обоснование влияния...",0.5,"Текст статьи...","Финансы","Интерфакс","2024-01-15"
```

- `id` - уникальный идентификатор
- `reasoning` - экспертное обоснование влияния на рынок
- `answer` - численная оценка влияния (-1 до 1)
- `article_text` - полный текст новости
- `sphere` - сфера экономики
- `source` - источник новости
- `date` - дата публикации

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `GIGACHAT_AUTH_KEY` | Base64(ClientID:ClientSecret) для OAuth | - |
| `GIGACHAT_CLIENT_ID` | ClientID для OAuth (если не используете AUTH_KEY) | - |
| `GIGACHAT_CLIENT_SECRET` | ClientSecret для OAuth (если не используете AUTH_KEY) | - |
| `GIGACHAT_SCOPE` | OAuth scope | `GIGACHAT_API_PERS` |
| `GIGACHAT_AUTH_URL` | Эндпоинт OAuth (NGW) | `https://ngw.devices.sberbank.ru:9443/api/v2/oauth` |
| `GIGACHAT_BASE_URL` | API база GigaChat | `https://gigachat.devices.sberbank.ru/api/v1` |
| `GIGACHAT_VERIFY_SSL` | Проверка TLS сертификата | `false` |
| `OPENAI_API_KEY` | API ключ OpenAI (опционально) | - |
| `DATA_PATH` | Путь к CSV файлу с данными | `data/mini_df.csv` |
| `CHROMA_DB_PATH` | Путь к базе ChromaDB | `./chroma_db` |
| `COLLECTION_NAME` | Название коллекции | `financial_news` |
| `REBUILD_COLLECTION` | Пересоздать коллекцию (сбросить и снова наиндексировать) | `false` |
| `EMBEDDING_MODEL` | Модель эмбеддингов HF | `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` |
| `LLM_MODEL` | Языковая модель | `gpt-4.1-mini-2025-04-14` |

### Настройка моделей

Поддерживаемые модели (по умолчанию):

- GigaChat (чат): `GigaChat-2-Pro` и др. из линейки GigaChat-2
- Эмбеддинги (локально): `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` (можно заменить на другой sentence-transformers)

Fallback OpenAI для чата при наличии `OPENAI_API_KEY` сохранён.

Важно: если ранее коллекция ChromaDB создавалась с другими эмбеддингами (например, OpenAI/GigaChat), рекомендуется пересоздать коллекцию под новую модель, иначе качество поиска ухудшится. Удалите содержимое каталога `chroma_db` или используйте новое имя коллекции (`COLLECTION_NAME`).

- **Эмбединги**: `text-embedding-3-small`, `text-embedding-3-large`
- **LLM**: `gpt-4.1-mini-2025-04-14` (по умолчанию), `gpt-4.1`, `gpt-4o-mini`, `gpt-4o`, `gpt-3.5-turbo`

## 📝 Примеры использования

### Поиск по финансовым новостям

```
👤 Ваш вопрос: Найди информацию о банке ВТБ

🤖 Ответ:
Вот информация о банке ВТБ из нашей базы:
1. ВТБ планирует увеличить кредитный портфель на 15% в 2024 году...
2. Банк ВТБ запустил новую программу льготного кредитования...
```

### Системная статистика

```
👤 Ваш вопрос: Покажи загрузку системы

🤖 Ответ:
Статистика системы:
🖥️  CPU: 23.5%
💾  Память: 45.2% (7.24 ГБ / 16.00 ГБ)
    Доступно: 8.76 ГБ
```

### Московское время

```
👤 Ваш вопрос: Сколько сейчас времени в Москве?

🤖 Ответ:
Текущее время в Москве: 2024-08-11 15:30:45 MSK
```

## 🔍 Логирование

Система ведет подробные логи всех операций:

```
2024-08-11 15:30:45 | INFO | 👤 Пользователь: Найди новости о Сбербанке
2024-08-11 15:30:46 | INFO | 🔧 Вызов функции: search_financial_news
2024-08-11 15:30:47 | INFO | 🔍 Поиск по запросу: 'Сбербанк' (top_k=5)
2024-08-11 15:30:48 | INFO | ✅ Найдено 5 результатов
2024-08-11 15:30:49 | INFO | 🤖 Ответ системы: Вот последние новости о Сбербанке...
```

Как смотреть логи в Docker:

- Интерактивный запуск (`docker compose run --rm chatbot`): логи видны сразу в текущем терминале.
- Постоянный запуск (`docker compose up --build`):
  - Просмотр в реальном времени: `docker compose logs -f chatbot`
  - Либо по имени контейнера: `docker logs -f financial_chatbot`
  - Сохранить логи в файл: `docker compose logs chatbot > logs.txt`

## 🐛 Устранение неполадок

### Ошибка "API ключ не указан"

```bash
# Проверьте файл .env
cat .env

# Убедитесь, что ключ указан правильно
OPENAI_API_KEY=sk-...
```

### Ошибка подключения к OpenAI API

- Проверьте интернет-соединение
- Убедитесь, что API ключ действующий
- Проверьте баланс на аккаунте OpenAI

### Проблемы с ChromaDB

```bash
# Удалить существующую базу и пересоздать
docker compose down -v
docker compose up --build
```

### Медленная работа

- Уменьшите параметр `top_k` в запросах
- Используйте более быструю модель эмбедингов
- Проверьте ресурсы системы

## 📊 Производительность

### Типичные показатели

- **Инициализация**: 10–30 секунд при первом запуске (создание эмбеддингов и индекса)
- **Поиск по RAG**: 1-3 секунды
- **Системная статистика**: <1 секунды
- **Московское время**: <1 секунды

### Требования к ресурсам

- **CPU**: 2+ ядра
- **RAM**: 4+ ГБ
- **Диск**: 1+ ГБ (для ChromaDB)
- **Сеть**: стабильное интернет-соединение

## 🔒 Безопасность

- API ключи хранятся в переменных окружения
- Технические детали скрыты от пользователя
- Валидация пользовательского ввода
- Обработка ошибок без раскрытия внутренней логики

Рекомендация: не коммитьте файл `.env` в репозиторий. Используйте `.env.example` как шаблон и задавайте реальные значения локально.

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

Этот проект создан для демонстрационных целей в рамках тестового задания.

---

## 🔗 E2E проверка GigaChat через Docker

Тесты ниже подтверждают, что приложение внутри Docker:
- успешно инициализируется,
- отвечает через API GigaChat,
- реально вызывает пользовательские функции (Function Calling).

Требуется корректно заполненный `.env` для GigaChat (см. раздел «Переменные окружения»).

Быстрый запуск E2E-тестов:

```bash
# Вариант 1: однокомандный скрипт (на хосте)
chmod +x scripts/ci_verify.sh
./scripts/ci_verify.sh

# Вариант 2: напрямую, из контейнера
docker compose run --rm \
  -e REBUILD_COLLECTION=true \
  chatbot \
  python tests/test_gigachat_live.py
```

Выходной отчёт сохраняется в `test_results/gigachat_live.json`.

Примечания:
- Если GigaChat-провайдер не настроен, E2E-тест будет корректно пропущен (код 0).
- Первый прогон может занять дольше (загрузка модели эмбеддингов HF и индексирование).

**Автор**: AI Engineer Test Task  
**Версия**: 1.0.0  
